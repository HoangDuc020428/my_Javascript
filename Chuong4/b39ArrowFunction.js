// arrow function: anonymus function (hàm không tên)
// viết bth
let square = function (x) {
  return x * x;
};
console.log(square(5)); //25

// viết theo kiểu arrow function
let square1 = (x) => {
  return x * x;
};

// rút gọn khi return
const square2 = (x) => x * x;

// arrow function khi xử lý nhiều dòng

const square3 = (x) => {
  // if else .....code here
};
